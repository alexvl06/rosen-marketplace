import { ImgURLCustomizerPipe } from './img-urlcustomizer.pipe';

describe('ImgURLCustomizerPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgURLCustomizerPipe();
    expect(pipe).toBeTruthy();
  });
});
